server:
  port: 80
  context-path: /

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yufengerp
    username: root
    password: admin123456
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
  thymeleaf:
    cache: false